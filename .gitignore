# Dependencies — these are always regenerated from package.json via npm install
node_modules/

# Build output
frontend/dist/
backend/dist/

# Environment variables — NEVER commit this; it contains API keys
.env
.env.local

# Private prompt files — LLM instructions should not be public
# Copy prompts.local.example.ts → prompts.local.ts to get started
*.local.ts

# Editor/OS noise
.DS_Store
*.swp
.vscode/
